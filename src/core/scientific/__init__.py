"""
Scientific research core modules.

Callers use the two orchestrators directly:
- `AcademicSearchOrchestrator` from `.search.orchestrator` (papers)
- `DatasetDiscoveryOrchestrator` from `.datasets.orchestrator` (datasets)

Both are wired into the `scientific_research` MCP tool in
`src/tools/scientific.py`.
"""
