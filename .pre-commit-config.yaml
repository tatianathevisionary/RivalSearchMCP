repos:
  - repo: https://github.com/psf/black
    rev: "26.1.0"
    hooks:
      - id: black
        args: [--line-length=100]

  - repo: https://github.com/pycqa/isort
    rev: "6.0.1"
    hooks:
      - id: isort
        args: [--profile=black, --line-length=100]

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: "v0.15.2"
    hooks:
      - id: ruff
        args: [--fix]

  # mypy is available but not wired into pre-commit: the codebase currently
  # has pre-existing type-check debt that a per-commit gate would block work
  # on. Run it manually (`uv run mypy src/`) or enable this hook after the
  # first clean pass. See CLAUDE.md § Code Quality.

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-json
      - id: check-added-large-files
        args: [--maxkb=500]
